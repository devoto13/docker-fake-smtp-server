{
  "name": "docker-fake-smtp-server",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "start": "fake-smtp-server -s ${SMTP_PORT:-1025} --smtp-ip ${SMTP_IP:-0.0.0.0} --http-port ${HTTP_PORT:-1080} --http-ip ${HTTP_IP:-0.0.0.0} --whitelist ${WHITELIST:-''} --max ${MAX:-100} --auth ${AUTH:-''} --headers"
  },
  "dependencies": {
    "fake-smtp-server": "0.7.7"
  }
}
